# Pushbutton Template

Copy this folder into a target `.panel` folder and rename it to `<ToolName>.pushbutton`.

Required edits:

- Update `bundle.yaml`
- Replace the smoke-test logic in `script.py`
- Add icons and help content
- Register the tool in `tool_registry.json`

