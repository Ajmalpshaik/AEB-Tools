# Hooks

Use this folder for pyRevit hook scripts only when a workflow truly requires them.

Before adding a hook:

- Confirm the event is stable across supported Revit versions.
- Log the reason in `docs/compatibility/`.
- Add a regression note in `tests/regression/`.

