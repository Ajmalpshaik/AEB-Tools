# Startup Scripts

Place extension-wide startup scripts here only when required.

Guidelines:

- Keep startup logic minimal and deterministic.
- Prefer lazy execution inside tools rather than heavy startup hooks.
- Record any Revit-version condition in the tool compatibility note.

