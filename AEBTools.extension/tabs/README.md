# Tabs Planning Area

This folder is for planning notes, staging ideas, or future generators.

Important pyRevit rule:

- Live `.tab` folders must be created directly under `AEBTools.extension/`.
- Do not expect pyRevit to discover bundles kept only inside this `tabs/` folder.

Use the starter templates in `../ui_templates/`, then place the real bundle under the extension root.
