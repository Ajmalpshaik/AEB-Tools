<!--
Tool Name    : Room to Door Window Layout
Purpose      : Define the WPF layout for the door parameter assignment command window
Author       : Ajmal P.S.
Company      : AJ Tools
Version      : 1.0.2
Created      : 2026-04-21
Last Updated : 2026-04-22
Target       : Revit 2020-2027
Platform     : pyRevit / XAML
Dependencies : WPF, pyRevit window loader
Input        : User-selected tool settings and preview content
Output       : Rendered tool window layout with branded footer placement
Notes        : Footer area is reserved for AEB Tools ownership branding on custom windows
Changelog    : v1.0.2 - Added room-side point lookup with a legacy Revit fallback mode
License      : All Rights Reserved
Repo         : AEB-Tools
-->
<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Room to Door"
    Height="710"
    Width="760"
    MinHeight="680"
    MinWidth="720"
    ResizeMode="CanResize"
    WindowStartupLocation="CenterOwner"
    ShowInTaskbar="False"
    Background="#FFF7F7F5">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="14" Background="#FFF1EFE6" BorderBrush="#FFD8D1B8" BorderThickness="1" CornerRadius="6">
            <StackPanel>
                <TextBlock FontSize="20" FontWeight="Bold" Foreground="#FF2F3B33" Text="Room to Door" />
                <TextBlock Margin="0,6,0,0" TextWrapping="Wrap" Foreground="#FF49554E"
                           Text="Writes a selected writable text door parameter using a room number from a door-side point lookup or the legacy Revit room fields plus an optional suffix. Whole Project is the default scope so existing doors are found without requiring a view-specific filter." />
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="0,14,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,0,4" FontWeight="SemiBold" Text="Scope" />
            <ComboBox Grid.Row="1" Grid.Column="0" Margin="0,0,14,10" x:Name="scopeComboBox" SelectionChanged="on_scope_changed" />

            <TextBlock Grid.Row="0" Grid.Column="1" Margin="0,0,0,4" FontWeight="SemiBold" Text="Destination Parameter" />
            <ComboBox Grid.Row="1" Grid.Column="1" Margin="0,0,14,10" x:Name="parameterComboBox" SelectionChanged="on_settings_changed" />

            <TextBlock Grid.Row="0" Grid.Column="2" Margin="0,0,0,4" FontWeight="SemiBold" Text="Separator" />
            <TextBox Grid.Row="1" Grid.Column="2" x:Name="separatorTextBox" MaxLength="12" TextChanged="on_settings_changed" />

            <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,4,0,4" FontWeight="SemiBold" Text="Room Source" />
            <TextBlock Grid.Row="2" Grid.Column="1" Margin="0,4,0,4" FontWeight="SemiBold" Text="Suffix Mode" />

            <ComboBox Grid.Row="3" Grid.Column="0" Margin="0,0,14,0" x:Name="roomSourceComboBox" SelectionChanged="on_room_source_changed" />
            <StackPanel Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,0,0" Orientation="Horizontal">
                <RadioButton x:Name="alphabeticRadioButton" Margin="0,0,18,0" GroupName="SuffixMode" Content="Alphabetic (A, B, C...)" Checked="on_settings_changed" />
                <RadioButton x:Name="numericRadioButton" GroupName="SuffixMode" Content="Numeric (1, 2, 3...)" Checked="on_settings_changed" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" Padding="12" Background="#FFFFFFFF" BorderBrush="#FFD8D8D8" BorderThickness="1" CornerRadius="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <CheckBox Grid.Column="0" x:Name="noSuffixSingleCheckBox" Content="No suffix for single door in room" Checked="on_settings_changed" Unchecked="on_settings_changed" />
                <CheckBox Grid.Column="1" x:Name="overwriteExistingCheckBox" Content="Overwrite existing values" Checked="on_settings_changed" Unchecked="on_settings_changed" />
            </Grid>
        </Border>

        <Grid Grid.Row="3" Margin="0,14,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Margin="0,0,0,6" FontWeight="SemiBold" Text="Preview Summary" />
            <TextBox Grid.Row="1"
                     x:Name="previewTextBox"
                     Padding="10"
                     Background="#FFFFFFFF"
                     BorderBrush="#FFD8D8D8"
                     BorderThickness="1"
                     FontFamily="Consolas"
                     FontSize="12"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     AcceptsReturn="True" />
            <TextBlock Grid.Row="2" x:Name="statusTextBlock" Margin="0,8,0,0" Foreground="#FF6A5F43" TextWrapping="Wrap" />
        </Grid>

        <Grid Grid.Row="4" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" VerticalAlignment="Center" Foreground="#FF5A5A5A"
                       Text="Run writes only the doors shown as update candidates in the preview." />
            <Button Grid.Column="1" x:Name="runButton" Width="110" Margin="0,0,10,0" IsDefault="True" Click="on_run_click" Content="Run Update" />
            <Button Grid.Column="2" Width="92" IsCancel="True" Click="on_cancel_click" Content="Cancel" />
        </Grid>

        <Border Grid.Row="5" Margin="0,12,0,0" Padding="0,10,0,0" BorderBrush="#FFE2DED1" BorderThickness="0,1,0,0">
            <TextBlock x:Name="brandingFooterTextBlock"
                       HorizontalAlignment="Center"
                       FontSize="11"
                       Foreground="#FF7A7467"
                       TextAlignment="Center"
                       TextWrapping="Wrap" />
        </Border>
    </Grid>
</Window>
